version: 0.2

phases:
  build:
    command:
      - echo Build Starting on `date`
      - chmod -x ./gradlew
      - ./gradlew build
      - cp
  post_build:
    commands:
      - echo Build completed on `date`
      - mv application.jar
      - pwd

artifacts:
  files:
    - Procfile
    - application.jar
    - .ebextensions/**/*
  discard-paths: yes

cache:
  paths:
    - '/root/.gradle/caches/**/*'
